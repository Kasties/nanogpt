[project]
name = "nanogpt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ipykernel>=7.1.0",
    "jax[tpu]>=0.6.2",
    "optax>=0.2.6",
    "torch>=2.10.0",
]
